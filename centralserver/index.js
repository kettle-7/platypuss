const fs = require('fs');
const http = require('http');
const { v4: uuidv4 } = require('uuid');

const hostname = '0.0.0.0';
const port = 3000;    // Change this variable to change the port for the server

